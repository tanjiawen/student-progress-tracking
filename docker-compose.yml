# 可选：快速启动开发依赖（取消注释所需服务）
version: '3.8'

services:
  # 示例：PostgreSQL
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: student
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: progress_tracking
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pg_data:/var/lib/postgresql/data

  # 示例：Redis
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

# volumes:
#   pg_data:
